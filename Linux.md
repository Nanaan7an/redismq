## Linux
### 文件系统目录结构
`/opt`:Linux用于安装其他软件的目录</br>
`/etc`:存放系统和程序所需的配置文件


### 常用命令
`tar zxf X.tar.gz`:z-解压gz；x-提取文件tar；f-指定名称</br>
`ps -ef | grep redis`:ps -ef：查询当前进程，且以-e所有进程和-f完整格式；|管道，把前面命令的输出作为后面命令的输入；grep：查询关键词
